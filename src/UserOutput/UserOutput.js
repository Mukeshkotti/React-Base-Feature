import React from 'react';

const userOutput = (props) => {
    return (
        <div>
            <p>{props.userName}</p>
            <p>This is a paragraph</p>
        </div>
    );
}

export default userOutput;